# conda env create --file environment.yml
name: igduran
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - fs
  - jupyter
  - jupyterlab
  - pathlib
  - pip
  - pyprojroot
  - python=3.11.7
  - pip:
    - pyhere
    